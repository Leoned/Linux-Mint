// Para ello se inicia el temrinal tecleamos lo siguiente

sdo apt-get --pure remove "programa"

// Lo que dice "programa" es poner nombre del programa a desinstalar por ejemplo GIM

// Luego tecleamos el siguiente comando para porder borrar las librerias y las dependencias del programa que hemos instalado y que ya no necestamos
